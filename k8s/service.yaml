apiVersion: v1
kind: Service
metadata:
  name: chatbot-backend-svc
  namespace: dev
spec:
  selector:
    app: chatbot-backend
  ports:
    - protocol: TCP
      port: 8282
      targetPort: 8282
  type: LoadBalancer
